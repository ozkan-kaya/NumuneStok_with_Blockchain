{
  "name": "blockchain",
  "version": "1.0.0",
  "description": "SupplyChainLedger Hardhat Project",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.22.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0"
  }
}
